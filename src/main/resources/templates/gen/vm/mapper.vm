package com.kalvin.kvf.mapper.$config.moduleName;

import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.metadata.IPage;
import com.kalvin.kvf.entity.sys.$config.firstCapFunName;

import java.util.List;

/**
 * <p>
 * $config.tableComment Mapper 接口
 * </p>
 * @since
 */
public interface ${config.firstCapFunName}Mapper extends BaseMapper<$config.firstCapFunName> {

    /**
     * 查询列表(分页)
     * @param ${config.funName} 查询参数
     * @return list
     */
    List<$config.firstCapFunName> select${config.firstCapFunName}List($config.firstCapFunName $config.funName, IPage page);

}
