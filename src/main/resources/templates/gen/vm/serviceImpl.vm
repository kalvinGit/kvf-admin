package com.kalvin.kvf.service.${config.moduleName};

import com.baomidou.mybatisplus.core.conditions.query.LambdaQueryWrapper;
import com.baomidou.mybatisplus.extension.plugins.pagination.Page;
import com.baomidou.mybatisplus.extension.service.impl.ServiceImpl;
import com.kalvin.kvf.entity.${config.moduleName}.${config.firstCapFunName};
import com.kalvin.kvf.mapper.${config.moduleName}.${config.firstCapFunName}Mapper;
import org.springframework.stereotype.Service;

import java.util.List;

/**
 * <p>
 * $config.tableComment 服务实现类
 * </p>
 *
 * @since $date
 */
@Service
public class ${config.firstCapFunName}ServiceImpl extends ServiceImpl<${config.firstCapFunName}Mapper, ${config.firstCapFunName}> implements I${config.firstCapFunName}Service {

    @Override
    public Page<${config.firstCapFunName}> listUserPage(${config.firstCapFunName} ${config.funName}) {
        Page<${config.firstCapFunName}> page = new Page<>(${config.funName}.getCurrent(), ${config.funName}.getSize());
        List<${config.firstCapFunName}> ${config.funName}s = baseMapper.select${config.firstCapFunName}List(${config.funName}, page);
        return page.setRecords(${config.funName}s);
    }

}
